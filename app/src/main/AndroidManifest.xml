<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arief.kasirsqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity android:name=".Login" > -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".MainActivity" />
        <!-- <activity android:name=".MainActivity"> -->
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".Add_Pegawai"></activity> -->
        <activity android:name=".Add_Pegawai" />
        <activity android:name=".Home" />
        <activity android:name=".DataMaster" />
        <activity android:name=".DaftarBarang" />
        <activity android:name=".FormBarang" />
        <activity android:name=".FormSupplier" />
        <activity android:name=".DaftarPegawai" />
        <activity android:name=".DaftarSupplier" />
        <activity android:name=".transaksi.HomeTransaksi" />
        <activity android:name=".Penjualan"></activity>
    </application>

</manifest>